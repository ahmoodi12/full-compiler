{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug main (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/main/compiler_main",
      "args": [
        "${workspaceFolder}/test/test_file.c"
      ],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build main",
      "terminal": "integrated"
    },
    {
      "name": "Debug Tests (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/test/compiler_tests",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build tests",
      "terminal": "integrated"
    }
  ]
}