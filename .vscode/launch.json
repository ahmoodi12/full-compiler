{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Tests (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/test/compiler_tests.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build tests",
      "terminal": "integrated"
    }
  ]
}